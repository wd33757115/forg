services:
  forge:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
      - ./.forge_state:/app/.forge_state
    command: ["python", "main.py", "--web", "--host", "0.0.0.0", "--port", "8000"]
